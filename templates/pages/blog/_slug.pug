extends /templates/layouts/default.pug

block content
  section
    .container
      article.lh-copy
        header
          h2 #{ post.data.title }

        content
          | !{ post.content }

        footer.mt4
          p 
            small.i 
              | Published #{ formatDate(post.data.date) }

          ul.tags
            for tag in post.data.tags
              li
                a(href="#") #{ tag }


